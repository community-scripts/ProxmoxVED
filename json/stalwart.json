{
  "name": "Stalwart",
  "slug": "stalwart",
  "categories": [
    0
  ],
  "date_created": "2026-08-03",
  "type": "ct",
  "updateable": true,
  "privileged": false,
  "architectures": ["amd64"],
  "interface_port": 8080,
  "documentation": "https://stalw.art/docs/",
  "website": "https://stalw.art/",
  "repository": "https://github.com/stalwartlabs/stalwart",
  "logo": "https://cdn.jsdelivr.net/gh/selfhst/icons@main/webp/stalwart.webp",
  "config_path": "/opt/stalwart_data/etc/config.json",
  "description": "Stalwart is an all-in-one mail and collaboration server written in Rust. A single binary provides SMTP, IMAP, POP3, JMAP, CalDAV, CardDAV and WebDAV plus spam filtering, DKIM/SPF/DMARC and a web admin interface, with no external database required for a default install.",
  "install_methods": [
    {
      "type": "default",
      "script": "ct/stalwart.sh",
      "config_path": "/opt/stalwart_data/etc/config.json",
      "resources": {
        "cpu": 2,
        "ram": 4096,
        "hdd": 20,
        "os": "Debian",
        "version": "13"
      }
    }
  ],
  "default_credentials": {
    "username": "admin",
    "password": null
  },
  "notes": [
    {
      "text": "On first start Stalwart runs in bootstrap mode and generates a temporary admin password. Read it with: journalctl -u stalwart | grep -A8 'bootstrap mode'. Then finish setup at http://<IP>:8080/admin.",
      "type": "warning"
    },
    {
      "text": "Running a mail server that is reachable from the internet needs correct DNS (MX, SPF, DKIM, DMARC, rDNS/PTR) and a valid TLS certificate. Set STALWART_HOSTNAME and STALWART_PUBLIC_URL in /opt/stalwart_data/etc/stalwart.env, then restart the service.",
      "type": "warning"
    },
    {
      "text": "Configuration and mail data live in /opt/stalwart_data (etc, data, logs), outside the app directory, so they survive updates. Include this directory in your backups.",
      "type": "info"
    },
    {
      "text": "The default datastore is embedded RocksDB - no external database needed. PostgreSQL, MySQL, SQLite, S3, Redis and NATS backends can be configured later through the admin UI.",
      "type": "info"
    }
  ]
}
