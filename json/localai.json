{
  "name": "LocalAI",
  "slug": "localai",
  "categories": [
    20
  ],
  "date_created": "2026-08-24",
  "type": "ct",
  "updateable": true,
  "privileged": false,
  "interface_port": 8080,
  "documentation": "https://localai.io/docs/",
  "website": "https://localai.io/",
  "repository": "https://github.com/mudler/LocalAI",
  "logo": "https://cdn.jsdelivr.net/gh/selfhst/icons@main/webp/localai.webp",
  "description": "LocalAI is a drop-in replacement for the OpenAI API that runs entirely on your own hardware. One binary serves text generation, image generation, speech-to-text and text-to-speech behind OpenAI-compatible endpoints, backed by a model gallery and pluggable inference backends. Heavier than llama.cpp, but multi-modal and self-managing.",
  "install_methods": [
    {
      "type": "default",
      "script": "ct/localai.sh",
      "config_path": "/opt/localai.env",
      "resources": {
        "cpu": 4,
        "ram": 8192,
        "hdd": 30,
        "os": "Debian",
        "version": "13"
      }
    }
  ],
  "default_credentials": {
    "username": null,
    "password": null
  },
  "app_vars": [
    {
      "name": "var_auth",
      "label": "Enable User Accounts",
      "type": "boolean",
      "default": "no",
      "help": "Installs PostgreSQL and enables the account system with roles and sessions. The first account created becomes admin, later ones need its approval"
    },
    {
      "name": "var_api_key",
      "label": "API Key",
      "type": "password",
      "secret": true,
      "help": "Shared key granting admin access, sent as a bearer token. Generated and saved to ~/localai.creds when left empty and user accounts are off"
    },
    {
      "name": "var_port",
      "label": "Port",
      "type": "number",
      "default": "8080"
    },
    {
      "name": "INSTALL_NVIDIA_DRIVERS",
      "label": "Install NVIDIA driver libraries",
      "type": "boolean",
      "default": "yes",
      "help": "Set to no when you install a matching driver in the container yourself"
    }
  ],
  "notes": [
    {
      "text": "No models are installed by default. Use the gallery in the web UI to pull one, or drop GGUF files into /opt/localai_data/models. Inference backends are fetched on first use and can be several GB each, which is what the 30 GB default disk is for.",
      "type": "info"
    },
    {
      "text": "The API is protected from the moment it starts. With user accounts off, a static API key is generated and saved to ~/localai.creds in the container; clients send it as 'Authorization: Bearer <key>' or an 'x-api-key' header, and it grants full admin access. With user accounts on, register in the web UI immediately after install - the first account created becomes the admin.",
      "type": "warning"
    },
    {
      "text": "GPU passthrough is offered during creation and driver libraries inside the container are matched to the host driver version automatically. Without a GPU everything still works on the CPU, just slowly.",
      "type": "info"
    },
    {
      "text": "Models, backends and the auth database live in /opt/localai_data and configuration in /opt/localai.env, both outside the application directory, so updates never touch them. A mount point on /opt/localai_data is worth considering rather than growing the root disk.",
      "type": "info"
    },
    {
      "text": "User accounts need a database. Upstream ships the release binary without the CGO auth tag, so the built-in SQLite store is unavailable and PostgreSQL is installed alongside LocalAI when the option is enabled. Leaving it off keeps the container to a single service.",
      "type": "info"
    }
  ]
}