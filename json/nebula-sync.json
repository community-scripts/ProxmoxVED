{
  "name": "nebula-sync",
  "slug": "nebula-sync",
  "categories": [
    5
  ],
  "date_created": "2026-08-03",
  "type": "ct",
  "updateable": true,
  "privileged": false,
  "has_arm": true,
  "interface_port": null,
  "documentation": "https://github.com/lovelaze/nebula-sync#configuration",
  "website": "https://github.com/lovelaze/nebula-sync",
  "logo": "https://cdn.jsdelivr.net/gh/selfhst/icons@main/webp/pi-hole.webp",
  "config_path": "/opt/nebula-sync.env",
  "description": "nebula-sync keeps multiple Pi-hole v6 instances in sync by replicating the configuration of a primary Pi-hole to one or more replicas over the Pi-hole API. It can run a full Teleporter import/export or sync only selected features, on a cron schedule.",
  "install_methods": [
    {
      "type": "default",
      "script": "ct/nebula-sync.sh",
      "config_path": "/opt/nebula-sync.env",
      "resources": {
        "cpu": 1,
        "ram": 512,
        "hdd": 2,
        "os": "Debian",
        "version": "13"
      }
    }
  ],
  "default_credentials": {
    "username": null,
    "password": null
  },
  "notes": [
    {
      "text": "This has no web interface. Edit /opt/nebula-sync.env and set PRIMARY and REPLICAS to your Pi-hole instances in the format 'http://host|password' (replicas comma-separated), then run 'systemctl restart nebula-sync'.",
      "type": "warning"
    },
    {
      "text": "Until PRIMARY and REPLICAS are configured the service restarts every 30 seconds. Check progress with 'journalctl -u nebula-sync -f'. The default schedule is CRON='0 * * * *' (hourly).",
      "type": "info"
    },
    {
      "text": "Requires Pi-hole v6 or newer on all instances - the v5 API is not supported. This is a third-party project and not part of the official Pi-hole project.",
      "type": "info"
    }
  ]
}
